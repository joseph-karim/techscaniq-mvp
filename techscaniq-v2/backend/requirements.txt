fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
aiohttp==3.9.1
python-dotenv==1.0.0
langchain==0.0.350
asyncio==3.4.3
python-multipart==0.0.6

# MCP-related dependencies
# Note: These are placeholders as actual MCP packages would need to be specified
# based on the actual MCP SDK/client library being used
# mcp-sdk==0.1.0  # Replace with actual MCP SDK when available